export default function About() {
  return (
    <div className="space-y-3">
      <h2 className="text-xl font-semibold">About</h2>
      <p className="opacity-80">
        This platform detects extreme heatwaves and computes human thermal stress
        indices (UTCI, WBGT, Heat Index), then issues graded early warnings.
      </p>
    </div>
  );
}
