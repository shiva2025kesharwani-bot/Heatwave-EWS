[project]
name = "heatwave-ews"
version = "0.1.0"
description = "Extreme heatwave early warning + thermal stress index"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests", "backend/tests"]
pythonpath = ["."]
